import React, { PropTypes } from 'react';

const propTypes = {
};

const defaultProps = {
};

const ${NAME} = (props) => {  
  return(
    <div></div>
  )
}

${NAME}.propTypes = propTypes;
${NAME}.defaultProps = defaultProps;
export default ${NAME};
